[package]
name = "tiger-perpetual"
version = "1.0.0"
edition = "2021"
description = "Perpetual futures trading for TigerSwap"

[dependencies]
tokio = { version = "1.35", features = ["sync", "rt"] }
serde = { version = "1.0", features = ["derive"] }
uint = "0.9"

[[bench]]
name = "perpetual_engine"
harness = false