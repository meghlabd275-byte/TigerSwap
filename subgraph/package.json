{
  "name": "tigerswap-subgraph",
  "version": "1.0.0",
  "description": "TigerSwap decentralized exchange subgraph for The Graph",
  "repository": "https://github.com/tigerswap/subgraph",
  "author": "TigerSwap",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/",
    "deploy-hosted": "graph deploy --node https://api.staging.thegraph.com/deploy/",
    "create-local": "graph create --node http://localhost:8020/ tigerswap",
    "remove-local": "graph remove --node http://localhost:8020/ tigerswap",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.70.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}